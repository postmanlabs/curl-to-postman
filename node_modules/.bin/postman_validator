#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../postman_validator/bin/postman_validator" "$@"
  ret=$?
else 
  node  "$basedir/../postman_validator/bin/postman_validator" "$@"
  ret=$?
fi
exit $ret
